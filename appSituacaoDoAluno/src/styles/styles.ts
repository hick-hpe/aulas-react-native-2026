import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    padding: 40,
  },

  row: {
    flex: 1,
    flexDirection: "row",
    gap: 100
  },

  col: {
    flex: 1,
    padding: 10,
  }
});
